/**
 * M1 composition shell for the console application.
 *
 * The Angular console UI is implemented in later milestones (M5+). For M1 this
 * is only a placeholder entry point so the project, its Nx tags
 * (scope:console, type:app), and the enforced dependency boundaries exist.
 */
console.log('[linkops-console] M1 shell — UI implemented in a later milestone.');
