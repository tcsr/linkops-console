# console-feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build console-feature` to build the library.

## Running unit tests

Run `nx test console-feature` to execute the unit tests via [Jest](https://jestjs.io).
